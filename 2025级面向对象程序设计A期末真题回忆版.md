# **2025级面向对象程序设计A期末真题回忆版**

一、判断对错并且说明理由(共20分)（顺序随机了，一共十道题，只写笔者记得的题，没写的就是忘了）

1，如果一个类不能实例化为对象，那么它一定是抽象类。

2、一个类中的数据成员的类型不能是该类的类型。

3、基类中的数据成员可以由派生类初始化。

4、成员函数void f(int *p)和void f(int *p)const可以构成重载。

5，某个类中若其所有构造函数都设为私有成员，那么它不能被实例化。



二、简答题（30分）（说明同上）

1，请说出<>这个运算符在c++语言中的全部作用以及它和()的关系。

2，既然函数的类型都是确定的，为什么还需要建立虚函数表呢？

3，分析一下全局变量，局部动态变量，局部静态变量，全局静态变量的生命周期。

4，c++语言中包含所谓“值的变化”，“类型的变化”，“空间的变化”，“代码的变化”。请分析c++用哪些语法支持了上述的变化？

5，试举例子说明一个类如何实现才能不被拷贝构造和赋值。

6，为什么传递给拷贝构造函数的参数需要是引用类型呢？

7，A是一个自定义的类，有如下代码：A(int i):f(i){ }  ，试着分析f可能是什么语言成分？





三、请写一段文字展现你对专业前景的展望，要求包含如下词语：C++，具身人工智能，量子技术，核聚变，硅基生物，软件工程，文化。



四、改错题，假设程序已经正确地包含了所有头文件以及命名空间等限制。请你找出程序错误，说明错误原因并且给出修改建议。前两道一道2分只有一个错误，最后一道4分有两个错误（一共四道题，只写记得的）

1.

```c++
class A{
private:
int k;
int *pa;
public:
A(int k=9){pa=new int [k];}

};
int main()
{
    A*s=new A;
    delete s;
    return 0;
}
```

2，

```c++
class A{
private:
int x;
public:
void f()const{cout<<x<<endl;}
void g(){cout<<x<<endl;}
};
```

3,

```c++
class A{
    public:
virtual void f()=0;
virtual ~A(){}
};
class B:public A{
    private:
    A a;
    A*pa;
    public:
    void f(){pa=new B;}
}
```

4,

```c++
class A{
    private:
     int x;
    public:
static void f(){cout<<x<<endl;}
};
class B:public A{
    private:
    int y;
    public:
    void g(){cout<<x<<y<<endl;}
}
```

五、

中东冲突日益严重。战争期间战斗机和导弹制备常态化。战斗机可以分为苏-27，苏-35，F-16，F-22,F-35等型号，战斗机需要装备的导弹可以分为战斧式导弹，海燕式导弹，流星式导弹，等类型。需要设计一个适配器，使得任何类型的导弹都能与任何类型的战斗机相适配，并且能够实现机械装载功能，数据监测功能以及控制功能等。要求你给出相应的类的接口的设计，不需要详细具体的实现，所实现程序需要满足可复用、可拓展、可维护的特点。（8分）



六、请你根据下面main函数中的调用设计和实现Set集合类，可以用STL中的vector容器实现。（10分）

```c++
int main{
    Set a;//构造空集合
    cout<<a.id;//每个集合需要有一个唯一的ID，ID构造规则可以自由发挥，但是要求必须和集合生成的顺序有关
    a.add(9);//向集合中加入整数9
    Set b(a);//构造集合b并且将a中所有元素复制过去，注意两个集合id不能相同
    a=a+b;//将集合a和b的元素拼在一起再赋值给a（不用考虑集合中元素需要是互异的性质）
    cout<<a;//输出a中所有元素   
}
```

七、每个公司都有总部、多个分公司、多个办事处等，为上下级同构实体关系。
   每个实体均有人力部、财务部、销售部等职能部门，不再有下级部门。分公司下面继续有子公司。
   总部需要快速了解总公司的：组织结构+实体职责展示。要求你提供一个公司各个部门的完全遍历，包括打印等功能。请给出相关类的设计和实现，不用实现main函数（8分）



八、阅读程序并且写出结果：

1，

```c++
#include <iostream>
using namespace std;

class A {
public:
static int count;
    A() {
        cout << "A" << ++count<<endl;
    }

    virtual ~A() {
        cout << "A" <<--count<< endl;
    }
};
int A::count=0;
class B : public A {
private:
    A a;
    A* pa;

public:
    B() {
        cout << "B" << endl;
        pa = new A[2];
    }

    ~B() {
        cout << "B" << endl;
        delete[] pa;
    }
};

void out(A*a)
{
delete a;
}
int main() {
   out(new B);
    return 0;
}
```

2，分别写出在表一，表二，表三，三种情况下main函数中的调用结果

```c++
表一
class A{
    public:
    void g()
    {
        f();
    }
    virtual void f()
    {
        cout<<"A::f"<<endl;
    }
    virtual~A(){}
};
class B:public A{
    public:
    
    void f()
    {
        cout<<"B::f"<<endl;
    }
    virtual~B(){}
};
```

```c++
表二
class A{
    public:
    virtual void g()
    {
        f();
    }
    virtual void f()
    {
        cout<<"A::f"<<endl;
    }
    virtual~A(){}
};
class B:public A{
    public:
     void g()
    {
        f();
    }
    void f()
    {
        cout<<"B::f"<<endl;
    }
    virtual~B(){}
};
```

```c++
表三
class A{
    public:
    virtual void g()
    {
        f();
    }
    void f()
    {
        cout<<"A::f"<<endl;
    }
    virtual~A(){}
};
class B:public A{
    public:
     void g()
    {
        f();
    }
    void f()
    {
        cout<<"B::f"<<endl;
    }
    virtual~B(){}
};
```

```c++
int main()
{
    B s;
    A&t=s;
    s.g();
    t.g();
    return 0;
}
```

